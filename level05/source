#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

int main(void)
{
    char s[100];
    unsigned int i;

    i = 0;
    fgets(s, 100, stdin);
    for (i = 0; i < strlen(s); ++i)
    {
        s[i] = tolower(s[i]);
    }
    printf(s);
    exit(0);
}
